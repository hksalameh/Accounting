<UserControl x:Class="AccountingApp.FuelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AccountingApp"
             FlowDirection="RightToLeft"
             Background="#F1F5F9"
             Loaded="FuelView_Loaded">
    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#059669"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#D97706"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#DC2626"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#64748B"/>
        <SolidColorBrush x:Key="TextBrush" Color="#0F172A"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#64748B"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E2E8F0"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" Opacity="0.05" BlurRadius="12"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="FieldTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="9,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="FieldComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource AppButtonStyle}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style x:Key="SuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
        </Style>
        <Style x:Key="EditButton" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="{StaticResource WarningBrush}"/>
        </Style>
        <Style x:Key="DeleteButton" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#EEF2F7"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="RowHeight" Value="38"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>
    </UserControl.Resources>

    <TabControl Margin="10" FontSize="14" FontWeight="SemiBold">
        <TabItem Header="فواتير الوقود">
            <Grid Margin="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- رأس الصفحة -->
                <Border Grid.Row="0" Style="{StaticResource CardStyle}" Padding="18,12">
                    <DockPanel LastChildFill="True">
                        <StackPanel DockPanel.Dock="Right">
                            <TextBlock Text="فواتير الوقود" FontSize="25" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                            <TextBlock Text="إضافة الفواتير ومتابعة المدفوع وغير المدفوع من شاشة واحدة" Foreground="{StaticResource MutedBrush}" Margin="0,3,0,0"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                            <TextBlock Text="السنة المالية" Style="{StaticResource LabelStyle}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox x:Name="YearComboBox" Width="105" Style="{StaticResource FieldComboBoxStyle}" Margin="0" SelectionChanged="RefreshData_Event"/>
                        </StackPanel>
                    </DockPanel>
                </Border>

                <!-- مساحة العمل -->
                <Grid Grid.Row="2" FlowDirection="LeftToRight">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MinWidth="620"/>
                        <ColumnDefinition Width="14"/>
                        <ColumnDefinition Width="345"/>
                    </Grid.ColumnDefinitions>

                    <!-- البحث والملخص والنتائج -->
                    <Grid Grid.Column="0" FlowDirection="RightToLeft">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <DockPanel Margin="0,0,0,10">
                                    <StackPanel DockPanel.Dock="Right">
                                        <TextBlock Text="بحث الفواتير" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                                        <TextBlock Text="حدد السيارة أو الفترة أو اعرض غير المدفوع فقط" Foreground="{StaticResource MutedBrush}" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <Button Content="طباعة" Style="{StaticResource SuccessButtonStyle}" Width="88" Height="36" DockPanel.Dock="Left" Margin="0" Click="Print_Click"/>
                                </DockPanel>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1.4*"/>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                        <TextBlock Text="السيارة" Style="{StaticResource LabelStyle}"/>
                                        <ComboBox x:Name="SearchCarComboBox" Style="{StaticResource FieldComboBoxStyle}" Margin="0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Margin="0,0,8,0">
                                        <TextBlock Text="من تاريخ" Style="{StaticResource LabelStyle}"/>
                                        <TextBox x:Name="SearchFromDateTextBox" Style="{StaticResource FieldTextBoxStyle}" Margin="0" TextAlignment="Center" KeyDown="DateInput_KeyDown"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" Margin="0,0,8,0">
                                        <TextBlock Text="إلى تاريخ" Style="{StaticResource LabelStyle}"/>
                                        <TextBox x:Name="SearchToDateTextBox" Style="{StaticResource FieldTextBoxStyle}" Margin="0" TextAlignment="Center" KeyDown="DateInput_KeyDown"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="3" VerticalAlignment="Bottom">
                                        <CheckBox x:Name="ShowUnpaidOnlyCheckBox" Content="غير المدفوع فقط" Margin="4,0,4,8" Checked="RefreshData_Event" Unchecked="RefreshData_Event"/>
                                    </StackPanel>
                                </Grid>

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,9,0,0">
                                    <Button Content="بحث" Style="{StaticResource PrimaryButtonStyle}" Width="72" Margin="0,0,4,0" Click="RefreshData_Event"/>
                                    <Button Content="عرض الكل" Style="{StaticResource SecondaryButton}" Width="90" Margin="0" Click="ShowAll_Click"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- ملخص سريع مضغوط -->
                        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="14">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="130"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="105"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="105"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="ملخص الاستهلاك" FontWeight="Bold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <ComboBox x:Name="SummaryCarComboBox" Grid.Column="1" Height="34"/>
                                <TextBox x:Name="SummaryFromDateTextBox" Grid.Column="3" Height="34" TextAlignment="Center" KeyDown="DateInput_KeyDown" ToolTip="من تاريخ"/>
                                <TextBox x:Name="SummaryToDateTextBox" Grid.Column="5" Height="34" TextAlignment="Center" KeyDown="DateInput_KeyDown" ToolTip="إلى تاريخ"/>
                                <Button Grid.Column="6" Content="احسب" Style="{StaticResource PrimaryButtonStyle}" Width="72" Height="34" MinHeight="34" Margin="8,0,0,0" Click="CalculateTotal_Click"/>
                                <Border Grid.Column="7" Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="8" Padding="12,6" Margin="12,0,0,0">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="الإجمالي: " Foreground="#1E40AF" FontWeight="SemiBold"/>
                                        <TextBlock x:Name="TotalAmountTextBlock" Text="0.000" Foreground="#1D4ED8" FontWeight="Bold" FontSize="16"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>

                        <Border Grid.Row="4" Style="{StaticResource CardStyle}" Padding="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="8"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <DockPanel Grid.Row="0">
                                    <TextBlock Text="الفواتير" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Right"/>
                                    <TextBlock Text="يمكن التعديل أو الحذف من نفس الصف" Foreground="{StaticResource MutedBrush}" DockPanel.Dock="Left"/>
                                </DockPanel>
                                <DataGrid x:Name="FuelInvoicesDataGrid" Grid.Row="2" AutoGenerateColumns="False">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="م" Binding="{Binding Id}" Width="50"/>
                                        <DataGridTextColumn Header="السيارة" Binding="{Binding CarNumber}" Width="90"/>
                                        <DataGridTextColumn Header="التاريخ" Binding="{Binding Date, StringFormat={x:Static local:AppSettings.DateFormat}}" Width="95"/>
                                        <DataGridTextColumn Header="رقم الفاتورة" Binding="{Binding InvoiceNumber}" Width="100"/>
                                        <DataGridTextColumn Header="المبلغ" Binding="{Binding Amount, StringFormat=N3}" Width="90"/>
                                        <DataGridTemplateColumn Header="مدفوع" Width="65">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold">
                                                        <TextBlock.Style>
                                                            <Style TargetType="{x:Type TextBlock}">
                                                                <Setter Property="Text" Value="لا"/>
                                                                <Setter Property="Foreground" Value="#DC2626"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsPaid}" Value="True">
                                                                        <Setter Property="Text" Value="نعم"/>
                                                                        <Setter Property="Foreground" Value="#059669"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn Header="غير المدفوع المتراكم" Binding="{Binding AccumulatedBalance, StringFormat=N3}" Width="125"/>
                                        <DataGridTemplateColumn Header="إجراء" Width="126">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                        <Button Content="تعديل" Style="{StaticResource EditButton}" Padding="8,1" MinHeight="27" Margin="2" Click="EditInvoice_Click"/>
                                                        <Button Content="حذف" Style="{StaticResource DeleteButton}" Padding="8,1" MinHeight="27" Margin="2" Click="DeleteInvoice_Click"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- إضافة فاتورة ثابتة -->
                    <Border Grid.Column="2" Style="{StaticResource CardStyle}" FlowDirection="RightToLeft" Padding="18">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0">
                                <TextBlock Text="إضافة فاتورة وقود" FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                                <TextBlock Text="أدخل بيانات الفاتورة ثم حدد حالة الدفع" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0"/>
                            </StackPanel>
                            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Margin="0,2,4,4">
                                    <TextBlock Text="رقم السيارة" Style="{StaticResource LabelStyle}"/>
                                    <ComboBox x:Name="InvoiceCarComboBox" Style="{StaticResource FieldComboBoxStyle}"/>

                                    <TextBlock Text="تاريخ الفاتورة" Style="{StaticResource LabelStyle}"/>
                                    <TextBox x:Name="InvoiceDateTextBox" Style="{StaticResource FieldTextBoxStyle}" TextAlignment="Center" KeyDown="InvoiceDate_KeyDown"/>

                                    <TextBlock Text="رقم الفاتورة" Style="{StaticResource LabelStyle}"/>
                                    <TextBox x:Name="InvoiceNumberTextBox" Style="{StaticResource FieldTextBoxStyle}" KeyDown="MoveFocusOnEnter"/>

                                    <TextBlock Text="المبلغ" Style="{StaticResource LabelStyle}"/>
                                    <TextBox x:Name="AmountTextBox" Style="{StaticResource FieldTextBoxStyle}" TextAlignment="Center" KeyDown="MoveFocusOnEnter"/>

                                    <Border Background="#F8FAFC" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,2,0,12">
                                        <CheckBox x:Name="IsPaidCheckBox" Content="تم دفع هذه الفاتورة" FontWeight="SemiBold"/>
                                    </Border>

                                    <Button x:Name="AddUpdateButton" Content="إضافة فاتورة" Style="{StaticResource SuccessButtonStyle}" Height="43" Margin="0,2,0,6" Click="AddUpdateInvoice_Click"/>
                                    <Button x:Name="ClearButton" Content="مسح الحقول" Style="{StaticResource SecondaryButton}" Height="38" Margin="0" Click="ClearFields_Click"/>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </TabItem>

        <TabItem Header="إدارة السيارات">
            <Grid Margin="20" MaxWidth="760" HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="14"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="إضافة سيارة" FontSize="19" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                            <TextBox x:Name="NewCarNumberTextBox" Style="{StaticResource FieldTextBoxStyle}" Margin="0,10,12,0" FontSize="16" KeyDown="MoveFocusOnEnter"/>
                        </StackPanel>
                        <Button Grid.Column="1" Content="إضافة السيارة" Style="{StaticResource SuccessButtonStyle}" Width="120" Height="40" VerticalAlignment="Bottom" Margin="0" Click="AddCar_Click"/>
                    </Grid>
                </Border>

                <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="10">
                    <DataGrid x:Name="CarsDataGrid" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="رقم السيارة" Binding="{Binding CarNumber}" Width="*"/>
                            <DataGridTemplateColumn Header="إجراء" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="حذف" Style="{StaticResource DeleteButton}" Padding="10,2" MinHeight="28" Margin="2" Click="DeleteCar_Click"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>
